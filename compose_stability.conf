// Shared Compose + compose-stability-analyzer stability configuration.
// Types listed here are treated as stable for skipping / stabilityCheck.
// Format: https://developer.android.com/develop/ui/compose/performance/stability/fix#configuration-file

// Kotlin ranges used as composable params (IntRange is inferred unstable by default).
kotlin.ranges.IntRange
kotlin.ranges.ClosedRange
kotlin.ranges.ClosedFloatingPointRange
kotlin.ranges.OpenEndRange

// kotlinx.datetime (immutable value types)
kotlinx.datetime.Instant
kotlinx.datetime.LocalDate
kotlinx.datetime.LocalDateTime
kotlinx.datetime.LocalTime
kotlinx.datetime.DateTimePeriod
kotlinx.datetime.DatePeriod
kotlinx.datetime.TimeZone
kotlinx.datetime.FixedOffsetTimeZone
kotlinx.datetime.UtcOffset
kotlinx.datetime.DayOfWeek
kotlinx.datetime.Month
